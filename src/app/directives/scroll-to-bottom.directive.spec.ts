import { PreventKeyseDirective } from "./prevent-keys.directive";


describe('FixedHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventKeyseDirective();
    expect(directive).toBeTruthy();
  });
});
