import { FixedHeaderDirective } from './fixed-header.directive';

describe('FixedHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new FixedHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
